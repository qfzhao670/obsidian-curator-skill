---
name: obsidian-curator
description: 整理、重排并美化 Obsidian 笔记（markdown），为笔记中不清楚的概念/术语打「解释补丁」，或将视频课程字幕直接生成中文 Obsidian 笔记。当用户要求整理/美化/重排 markdown、添加双链 wikilinks、标签 tags、callout、frontmatter、外部链接、解释概念术语，或把视频字幕整理成笔记时使用。
---

# Obsidian Curator

把「有内容但缺结构、不够美观」的 Obsidian 笔记，整理成**简洁、有条理、不花哨**的成品，
并加上 Obsidian 特有的知识关联能力（双链、标签、callout、frontmatter、外部链接）。
也可以把一段视频课程字幕（含时间戳的原始讲稿）直接生成一篇中文 Obsidian 笔记。

更详细的语法速查与成品示例见 `references/formatting-guide.md`；
字幕生成笔记的完整流程与示例见 `references/subtitle-to-note.md`。
（如果任务是字幕生成直接生成完整笔记，那么formatting-guide和subtitle-to-note都需要参考）

## 核心原则（优先级最高）

1. **不编造**：只重组、拆分、润色已有内容，绝不新增原文没有的事实、数据、例子或结论。唯一例外是「概念补丁」（见下）：用户点名要解释某个概念时，可新增解释，但必须明确标注为补丁、且只讲公认定义。
2. **层级克制**：正文只用 `##` / `###` 两级（极少数 `####`）；`#` 仅当笔记缺标题时补一个标题。不要多级嵌套、不要为了切分而切分。
3. **长段落 → 短段落 + 无序列表**：把并列的要点、步骤、属性拆成 `- ` 列表，一句话讲一件事。
4. **图片原样保留**：`![[...]]` 不动，就近放在相关段落旁，可补一行简短说明。
5. **高亮克制**：保留原文 `==高亮==`，用于关键术语；可适度补，但不泛滥。
6. **排版克制**：表格仅在「对比 / 多列数据」时用；callout 每篇最多 1–2 个；不添加表情、不滥用加粗/斜体。

## 工作流程

1. 读入目标（单篇笔记，或目录下多篇）。
2. 通读，提取主题结构：在讲什么、有哪些并列概念、段落之间什么关系。
3. 重排结构：定标题层级、把并列点拆成列表、相关段落归组。
4. 逐项应用下面的 Obsidian 特性。
5. 写回（见「写回约定」）。

## 从视频字幕生成笔记

用户提供一段**视频课程字幕**（多为英文、含时间戳，如 NoteGPT 导出的 `.txt`），
需要直接产出**一篇中文 Obsidian 笔记**。与「整理已有笔记」不同：输入是未经分段的原始讲稿，
没有现成标题，要先理解、切分、翻译，再套用后面的 Obsidian 特性。

### 输入格式
- NoteGPT：`时间戳行 + 文本行`，空行分隔（`00:00:00` 下一行是那段话）。
- 也可能是 SRT / VTT 等带时间码的字幕；统一**去掉时间戳、序号、空行**，拼成连续讲稿即可。
- 时间戳只用来大致判断节奏，**不要**按时间戳机械切段；分段靠理解内容脉络。

### 生成流程
1. **通览定题**：读完整段，判断主题和讲课脉络
   （常见：开场 → 定义 → 图解/例子 → 反面推演 → 正面结论 → 类型列举 → 功能 → 目标 → 小结）。
2. **清洗**：剔除口语填充词（Alright、So、you know…）、重复句、口误、无关寒暄；
   「稍后会在另一节讲 XX」这类预告浓缩成一句，不展开。
3. **切分结构**：按讲课脉络定 `##`/`###`，把并列的要点/例子/类型/功能拆成 `- ` 列表。
4. **翻译**：输出中文；专业术语**首次出现**写成「中文（English, 缩写）」，
   如「操作系统（Operating System, OS）」，之后用中文或缩写；译名要准确通用
   （CPU=中央处理器、RAM=随机存取存储器、ROM=只读存储器）。讲课者举的例子（Word、Excel、Chrome 等）保留。
5. **套用特性**：加 frontmatter（`source` 填视频/课程名）、`==高亮==` 关键术语、callout 小结（≤2 个）；
   `[[双链]]` 与外链规则同「Obsidian 特性」。
6. **写回**：**生成新文件**（不改字幕原文件），文件名用主题（如 `操作系统导论.md`）；
   默认输出到字幕同目录，用户指定 vault 路径则写那里。

### 边界
- **浓缩允许，编造禁止**：删废话、合并重复、理顺口语语序都算「重组」；
  不得新增讲稿里没有的事实、数据、例子或结论。
- **翻译忠实**：不添油加醋、不夹带观点；拿不准的译名用「中文（English）」并存。
- 语气是「干净的听课笔记」，不是逐字稿，也不保留时间戳。

## Obsidian 特性（默认全部启用；可按需关闭某项）

### 1. 内部 wikilinks（双链）
- 语法：`[[笔记名]]`、`[[笔记名#标题]]`、`[[笔记名#^块ID]]`、`[[笔记名|别名]]`。
- **链接要嵌在正文里、有实际关联意义的位置**，而不是在文末堆一个「相关笔记」列表——那种空泛的链接列表没有用处。
  - 一句话 / 一段 → 另一篇的某个标题：`[[笔记名#标题]]`
  - 某个标题 → 另一篇的某一段（块）：`[[笔记名#^块ID]]`
  - 一句话 → 另一整篇：`[[笔记名]]`
- 只链接 vault 中**真实存在**的笔记，或明确可作为独立条目的概念；无法确认存在时用别名形式（如 `[[RAM|内存]]`），宁缺毋滥。
- 只在**确有链接意义**时建链：知识库还小、笔记之间尚无实质关联时，不加内部链接是正常的，不要硬凑。
- 这是 Obsidian 图视角 / 反向链接的核心价值：把有真实关联的知识点连起来，而不是生造关系。

### 2. 标签 + frontmatter
- 文件顶部加 YAML frontmatter：`tags`（含嵌套，如 `#cs/hld`）、`aliases`、`source`（来源课程/视频等）。
- 正文中可为关键主题加 `#标签`。

### 3. Callout 摘要框
- `> [!note]` / `> [!tip]` / `> [!warning]` 用于「关键结论 / 总结 / 注意」，每篇最多 1–2 个。
- 语法：`> [!note] 标题` 起一行，后续内容每行以 `> ` 开头。

### 4. 外部链接
- `[文字](url)` 只链接**公认官方文档**：Jenkins、Nginx、Sentry、PM2、GitHub Actions、CAP 定理等有官方站点的内容。
- 拿不准的 URL 一律不加，绝不杜撰。

## 概念补丁（按需解释概念/术语）

整理完后，用户常会指着笔记里某句话问：「这里的『XX』是什么意思？」此时**不要重新整理整篇**，而是在**原句旁边打一个「补丁」**：用最简洁、通俗又严谨的话，把他问的那个概念讲清楚。

### 补丁格式
- 在原句**正下方**紧跟一个**默认折叠的 callout**，标题以 `补丁` 开头：
  ```markdown
  - 除了 IP 层还有**应用层**……
    > [!question]- 补丁：IP 层 / 应用层是什么？
    > 解释……
  ```
- 原句在列表里时，callout 缩进两个空格，保持列表不断开。
- 用 `[!question]-`（`-` 表示默认折叠），不打断阅读、也不与正文混淆。

### 解释怎么写
1. **通俗但严谨**：先说「它负责什么 / 解决什么问题」，再用一两个类比或例子；概念边界必须准确，不因通俗而失真。
2. **极简**：一般 2–4 句，只答用户问的那一个/几个概念，不展开无关内容。
3. **只讲公认定义**：不编造、不臆测；是概括时点明「大致可以理解为……」，拿不准就说明是简化。
4. **标注为补丁**：这是 Claude 新增的解释，与原文区分，方便用户日后删除或改回。
5. 补丁里的概念若与本库其他笔记相关，顺手加 `[[双链]]`；否则不必。

### 备选：脚注
解释希望「不占正文」时，可用脚注：原词旁标 `[^1]`，解释放文末 `[^1]: ……`。
默认仍用折叠 callout（离原句最近、自包含、一眼可见）。

### 常见问题类型
- 「XX 是什么」：一句话定义 + 它的职责/作用。
- 「XX 和 YY 有什么区别」：一句话点出最关键差异，必要时用对照。
- 「为什么需要 XX」：它解决什么痛点、没有它会怎样。

## 写回约定
- 默认就地编辑原文件。
- 用户要求时：输出到新文件，或只给出改动 diff、不落盘。
